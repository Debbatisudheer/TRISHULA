# TRISHULA V0.9.110.1 — STATUS

Status: IMPLEMENTED — awaiting UCRT64 validation

DSA: fixed-size sliding window / ring buffer
Scope: Ground Data Service telemetry operational projection
Default window: 32 samples
Complexity: O(1) update, O(1) rolling average, O(k) bounded memory

Regression scope preserved:
- V0.9.105.1 autonomous rover/science campaign
- V0.9.106.1 ground ingestion queue campaign
- V0.9.107.1 queue pressure campaign
- V0.9.108.1 command scheduler min-heap
- V0.9.109.1 mission-history Top-K heap

Required user validation:
- `go test ./...`
- telemetry sliding-window benchmark
- C++ `ctest --test-dir build --output-on-failure`
- existing DSA campaigns
