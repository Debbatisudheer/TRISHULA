# Optional. The default uses the Next.js same-origin proxy to avoid browser CORS.
# GROUND_API_URL=http://localhost:8082
# NEXT_PUBLIC_GROUND_API_URL=/api/ground  # HTTP proxy path; leave unset for default
NEXT_PUBLIC_MISSION_ID=TRISHULA

# WebSocket uses the configured ground API host when NEXT_PUBLIC_GROUND_API_URL is an absolute URL.
