# V0.9.119.1 — Ground Station Navigation

Adds the Ground Station Mission Control section on top of the V0.9.118.1 Alerts checkpoint.

## Real data sources
- `/v1/mission-control/dashboard`
- `/v1/mission-control/state`
- `/v1/missions/TRISHULA/telemetry?limit=50`
- `/v1/missions/TRISHULA/events?limit=50`
- `/v1/mission-control/alerts`
- runtime state already loaded from `/v1/missions/TRISHULA/runtime`

## UI behavior
- Ground Station is enabled in the navigation.
- Shows Ground API/live availability, WebSocket availability, mission projection state, latest source, accepted telemetry/events/commands/alerts, latest record identifiers, mission freshness/readiness/health, and runtime/telemetry sequence.
- The page does not invent Kafka/PostgreSQL/Redis connectivity states because the existing API does not expose direct component health endpoints.
- Missing backend evidence remains `—` or `WAITING`.
- `Sudheer.Dev` remains in navigation/footer.
